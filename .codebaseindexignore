# Repo-specific codebase-indexing excludes — IN ADDITION to the indexer's
# built-in defaults. Binary/lock/build/dependency/cache files and tooling-
# metadata dirs (.github, .husky, .cursor, .claude, .docker, node_modules,
# .turbo, dist, coverage, …) are already excluded globally by
# BUILTIN_EXCLUDE_PATTERNS, so only patterns UNIQUE to this repo belong here.
/docs
/pnpm-workspace.yaml
/release.config.cjs
/CLAUDE.md
/LICENSE
/README.md
/scripts
/assets
**/tsconfig.*
/turbo.json
**/vitest.config.*
**/cypress/api-definitions
